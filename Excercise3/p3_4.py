# Create the list and add the elements...
def insert1(l):
		k=[]
		k.append(l)
		print(k)
	#	k.insert(l)
		#k[0]=l		
		return k
while True:
	insert=input("Enter the ele u have to insert it.. = ")
	c=insert1(insert)
print(c)
